## Django backend base URL
# VITE_API_BASE_URL=http://localhost:8000

## Force mock data mode (if backend not running)
VITE_USE_MOCK=true

## Backend base URL (Django)
# VITE_API_BASE_URL=http://localhost:8000/api

## Use mock data in frontend (until backend is ready)
VITE_USE_MOCK=true

